def grid_to_particle(grid_output, particles, grid_size=(64, 64)):
    """
    Convert a grid output to particle values.

    :param grid_output: Grid output tensor of shape (batch_size, out_channels, height, width)
    :param particles: Particles tensor of shape (num_particles, in_channels)
    :param grid_size: Size of the grid (height, width)
    """
    # Implementation as shown above
    pass

def particle_to_grid(particles, grid_size=(64, 64)):
    """
    Convert particle values to a grid.

    :param particles: Particles tensor of shape (num_particles, in_channels)
    :param grid_size: Size of the grid (height, width)
    """
    # Implementation as shown above
    pass